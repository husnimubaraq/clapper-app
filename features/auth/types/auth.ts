export type TLoginRequest = {
    username: string
    password: string
}

export type TAuth = {
    id: string
    name: string
    nickname: string
    poin: number
    member_card: string
    token_device: string
}