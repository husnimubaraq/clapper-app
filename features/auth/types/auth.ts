export type TLoginRequest = {
    phone: string
    password: string
    token_device: string
}

export type TAuth = {
    id: string
    name: string
    nickname: string
    poin: number
    member_card: string
    token_device: string
}